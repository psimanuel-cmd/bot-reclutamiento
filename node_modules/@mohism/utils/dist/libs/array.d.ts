/**
 * 统计一个对象数组里，
 * @param arr 对象数组
 * @param key
 * @param trans
 * @returns
 */
export declare const countBy: (arr: {
    [key: string]: any;
}[], key: string, trans?: (key: string) => string) => {
    [key: string]: number;
};
