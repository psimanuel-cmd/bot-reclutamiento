"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.toabcDef = exports.toAbcDef = void 0;
/**
 * 将空格分隔的一组词，变成大驼峰模式
 * 如： here we are  =>  HereWeAre
 * @param input string
 */
const toAbcDef = (input) => {
    return input.split(' ').map((piece) => {
        return `${piece[0].toUpperCase()}${piece.substr(1)}`;
    }).join('');
};
exports.toAbcDef = toAbcDef;
/**
 * 将空格分隔的一组词，变成小驼峰模式
 * 如： here we are  =>  hereWeAre
 * @param input string
 */
const toabcDef = (input) => {
    return input.split(' ').map((piece, index) => {
        return `${index > 0 ? piece[0].toUpperCase() : piece[0].toLowerCase()}${piece.substr(1)}`;
    }).join('');
};
exports.toabcDef = toabcDef;
//# sourceMappingURL=formatString.js.map